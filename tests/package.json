{
  "name": "@vibemeter/tests",
  "version": "0.1.0",
  "private": true,
  "description": "All VibeMeter tests — unit, integration, e2e, stress",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@vibemeter/core": "workspace:*",
    "@vibemeter/hooks": "workspace:*",
    "@vibemeter/cli": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "*",
    "tmp": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
